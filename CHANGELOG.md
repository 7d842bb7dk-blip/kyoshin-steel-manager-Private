# 変更履歴（CHANGELOG）

形式は緩やかに [Keep a Changelog] に倣います。日付は JST。

## [1.18.0] - 2026-09-24（準備完了・ツール実行待ち）
会社のドメイン（エックスサーバー）で正式な証明書を取る方式に。新アドレス予定 https://zaiko.f-kyo-shin.co.jp/

### Added
- **エックスサーバー DNS 対応**（XServer API・公式仕様 openapi.json 準拠）。ホームページ・メールを壊さないため、
  触るのは `zaiko` の A（自分で作ったものだけ）と `_acme-challenge.zaiko` の TXT だけ。
  一覧が正しく読めない・同じ名前に他の設定がある・予約名（www/mail 等）・多段名は書き込まずに中止。
- 設定ツールがAPIキーからサーバーを自動検出（キーの権限で見えなければ手入力）・キーの有効期限を警告。
- 管理者画面に、証明書の自動更新が失敗・期限14日前のときの赤い警告帯（/api/health に tls 状態）。
- 手順書 `docs/証明書の設定手順（会社ドメイン）.txt`（業者への依頼文つき）。

### Changed（レビューで確定した問題の修正）
- 社内の名前解決がこのサーバーを指すまでドメインへ転送しない（切り替え直後に全端末が使えなくなる問題）。
  名前解決の一時失敗では切り替えず、ドメインで届いた要求は追い返さない。
- 設定（acme.json）は本番の証明書取得に成功した後にだけ保存。サーバーは初回取得をせず、既存証明書の更新のみ。
- 期限切れ・名前違いの証明書は使わない（IPのアドレスで動き続ける）。ディスク上の新しい証明書は自動で読み直し。
- 更新失敗は6時間あけて再試行（再起動をまたいで記録）。同時実行ロック（持ち主が居ない残骸は無視）。
  更新中は自動反映の再起動を見送る。CSR作成の例外でサーバーが落ちる問題を修正。
- 同じ名前の有効な証明書があれば取り直さない（発行枚数制限対策）。予行演習の古い値のキャッシュ対策。
- 開発用サーバー（dev.js）は証明書まわりを本番と共有しない。

---

## [1.17.0] - 2026-09-24（準備のみ・未稼働）
各端末の証明書設定をなくすための仕組みを用意（設定するまで動作は従来どおり）。

### Added
- `server/acme.js`：Let's Encrypt 証明書の自動取得・自動更新（ACME/RFC 8555、DNS-01、外部パッケージなし）。
  期限30日前に12時間ごとの確認で自動更新し、再起動なしで差し替え。
- サーバーは Let's Encrypt 証明書があれば、そのドメイン宛てのアクセスに使う（SNIで切替）。
  正式アドレスがドメインに変わり、旧アドレス（IP・:3001・:80）はすべて自動転送（`?co=` 等は保持）。
- `tools/証明書を自動取得.bat`（`tools/domain-setup.js`）：ドメイン・トークン・規約同意を入力するだけの初期設定。
  予行演習（テスト用認証局）→本番取得→サーバーへ即反映→名前解決チェックまで自動。
- `POST /api/tls-reload`（サーバー機内からのみ）：証明書の読み直し。

### 注意
- 当初想定の DuckDNS は、社内のファイアウォール（FortiGate）の Webフィルターで
  「ダイナミックDNS」カテゴリとしてブロックされているため、このままでは使えない（2026-09-24 確認）。
  ファイアウォールでの許可、または独自ドメインの利用が必要。

---

## [1.16.2] - 2026-09-24
### Fixed（保管場所ガイドのレビュー指摘）
- **「Φ8.0」形式の規格が棚のサイズ表と一致せず、細い化粧管が C-7（Φ216.3専用）等へ誤案内される
  問題**を修正（寸法を数値で正規化して比較：Φ8.0＝8）。サイズ表に無い寸法は推測せず案内しない。
- **長さがどの棚の範囲にも入らないとき、短尺棚が優先される問題**を修正（HL アングル 5,200mm が
  E-7 になっていた → A-3）。長さが近い棚を最優先に。
- **今の棚が条件を満たしていれば動かさない**ように（本番600件中580件は現状維持を確認）。
- SS400 等の鉄系・仕上げ空欄のアングルは案内しない。チタンは C-3 のみ。混在棚 B-1・E-12 と
  C-3 の φ16 をPDFどおり追加。単体版（file://）ではガイド無効。
- 登録後の画面更新が失敗しても「失敗しました」と誤表示しない（記録は済んでいるため二重登録を防止）。

---

## [1.16.1] - 2026-09-24
### Changed
- **保管場所ガイドを「確認なしで決定」に**：選択ボタンを廃止し、判定した棚をそのまま登録に反映。
  残り長さの登録では長さと保管場所を1回の処理で更新（`/api/checkout` に `loc` を追加、
  履歴メモに「保管場所 A-4→E-7」を記録）。残材登録は選んだ場所より判定結果を優先して登録。
  案内画面は特大の「〇-〇 になおしてください」＋OKボタンのみ（変更時は「A-4 → E-7」を小さく表示）。
- 長尺アングルの棚を補完：A-2（不等辺・60角以上）・A-5（30角以下）・A-6（40角）を追加し、
  A-5〜A-7・E-1〜E-4 は HOT／未研／仕上げ空欄を対象に（HL・#400・COLDが誤って入らないよう）。

---

## [1.16.0] - 2026-09-23
保管場所ガイド／E棚アングル56本の取込／証明書の「安全ではありません」対策。

### Added
- **保管場所ガイド**：残材登録・残りの長さ登録の直後に、「材料保管場所_住所一覧」PDFの棚割り
  （材質×鋼種×仕上げ×サイズ×長さ帯、A〜H棚62区画）から置き場所を判定し、
  **「〇-〇 になおしてください」を特大表示**（日本語＋ベトナム語併記）。場所が違う場合は
  ワンタップで登録の保管場所も変更できる。長さ帯から外れる場合は最も近い棚を提案。
  SS400・フラットバー等、PDFに棚定義が無いものは案内しない。
- **在庫取込：E-1〜E-7 のアングル56本**（材料在庫_画像読取結果(2).xlsx の追加分。
  バックアップ backup_before_e17_20260923.json）。Excelで材質空欄のため**SUS304と仮置き**
  （履歴に要確認と記録）。仕上げの「片#400/両#400」は区別を保持。同一規格・同長さの
  実物2本もそれぞれ登録。在庫総数600件。E-10〜E-12の27件は既存と一致のため追加なし。
- **社内CA方式の証明書に切替**：`/ca.crt` から社内CA証明書を配布。各端末で1回
  インストール＋信頼すると**「安全ではありません」表示が完全に消える**。
  サーバー証明書は800日有効（iPhoneの825日制限対応・期限時は tools/TLS証明書を作る.bat）。

---

## [1.15.2] - 2026-09-23
### Changed
- **マニュアルを5ページ構成に拡充**：②を「前半・まず倉庫の鍵を借りる」「後半・残りの長さを登録」に
  分割し、鍵の手順を具体化（鍵置き場のイラスト＝フック＋貼り紙、かざす画面、押すボタン、
  帰ったらフックに掛けるだけ、まで1ページで図解）。①の絞り込み欄も実物どおり4項目
  （材質・鋼種・材料規格・保管場所）のスクリーンショットに修正。
- 鍵置き場に貼るQRポスターの文言を現仕様に修正（「行く前・返す時に」→「倉庫へ行く前に
  スマホでかざす」。返却操作は無いため）。

---

## [1.15.1] - 2026-09-23
### Changed
- **使い方マニュアルを「統一アドレス＋かざすだけスキャン」版に全面更新**（docs/マニュアル）：
  表紙に https://192.168.1.107/ と初回設定（証明書警告の許可・カメラ許可）を明記。
  ②在庫を使う はライブスキャナーの実画面入り。全ページはみ出し・重なり・ベトナム語検査済み。
- **持ち出し画面の「全部/一部」ボタンのレイアウト改善**：アイコンを上に、文字を中央2行に
  （「登録」が途中で折れて読みにくかった問題を解消。スマホ・PC共通）。

---

## [1.15.0] - 2026-09-23
材料規格候補をマスタで編集可能に。サニタリーパイプの肉厚・仕上げを整備。

### Added
- **材料規格候補がマスタ設定から編集可能に**（マスタ参照→「鋼種別 材料規格候補」）。
  カンマ区切りで追加・変更でき、規格が未設定だった鋼種（化粧管・BA管・丸棒・角棒）も
  行が出るのでそのまま追加できる。「鋼種を追加」で新しい鋼種の行も作れる。
  保存はこれまで同様パスコード必須・全端末に自動配信。
- **サニタリーパイプの板厚 自動補完**：呼び径(1S/1.5S/2S…)で外径と肉厚が決まる
  （JIS G3447）ため、在庫の追加/編集・残材登録・重量計算で規格（例: Φ50.8）を入れると
  板厚が自動で入る（手で選んだ値は上書きしない）。対応表:
  Φ25.4/31.8/38.1→t1.2、Φ50.8→t1.5、Φ63.5〜101.6→t2、Φ114.3/139.8→t3。

### Changed
- **在庫データ修正（バックアップ backup_before_sanfix_20260923.json）**：
  サニタリーパイプの板厚0を外径から補完（4件：Φ32→1.2、Φ50.8×2→1.5、Φ63.5→2）、
  表面仕上げ空欄を #400 に統一（16件）。
- サニタリーパイプの規格候補の既定値を JIS G3447 の外径（Φ25.4〜Φ76.3）に修正。

### Removed
- 「うまく読めないとき：写真から選ぶ」ボタンを削除（かざすだけスキャンで不要になったため。
  HTTP時やカメラ許可拒否時の撮影フォールバックは残る）。

---

## [1.14.1] - 2026-09-23
アドレスを **`https://192.168.1.107/` の1本に統一**（PC・スマホ共通、ポート表記なし）。

### Changed
- HTTPSを443で起動（使えなければ3443へ自動退避）。`http://…:3001`・`http://…:80` で開いても
  正式アドレスへ302転送（`?co=` 等のクエリも引き継ぐ）ので、**印刷済みラベルや古いブックマーク
  もそのまま使える**。`/api/*` は転送しないため、開きっぱなしの旧ページも壊れない。
- QRラベルの新規印刷分・アプリ内の誘導リンクは正式アドレスを指すように（QRも少し小さくなる）。
- 各端末で初回だけ証明書の警告を許可する（詳細→閲覧）。localStorage がアドレス単位のため、
  名前の自動選択と言語設定は初回に選び直し。

---

## [1.14.0] - 2026-09-23
スマホの「QRを読み取る」が**かざすだけの自動読み取り**に。鍵も材料もアプリだけで完結。

### Added
- **かざすだけQRスキャナー**：「QRを読み取る」を押すとカメラが起動し続け、QRを枠に入れるだけで
  自動検出（シャッター不要）。Android=端末内蔵デコーダ、iPhone=同梱zxing-wasmで毎秒約4回判定。
  ライト対応端末には点灯ボタンも表示。検出したら従来どおり持ち出し画面／鍵画面が直接開く。
- **HTTPS配信を追加**（`https://192.168.1.107:3443/`）：ブラウザのカメラAPIがHTTPS必須のため。
  自己署名証明書（`server/data/tls/`・Git対象外・10年有効）。IP変更時は
  `tools/TLS証明書を作る.bat` で再生成。http側(3001)は従来どおり（QRラベルのリンク先も変更なし）。
- httpのスマホ画面には、httpsアドレスへの誘導リンクを表示（初回だけ証明書警告→許可が必要）。
  httpや権限拒否時は従来の「撮影して読み取り」に自動フォールバック。

---

## [1.13.1] - 2026-09-23
現場からの「QRが読めない」の原因を特定するための診断機能。

### Added
- **読み取り失敗の自動診断**：スマホの「QRを読み取る」が失敗したとき、撮った写真と端末情報
  （機種・各デコーダの結果・画像サイズ）を `POST /api/scanfail` でサーバーへ自動送信し、
  `server/data/scanfail/` に保存（最新20件のみ・Git対象外）。管理者が原因を直接調べられる。
- 失敗時のメッセージを改善（「写真は調査用に送信済み」「明るい場所でラベル全体を大きく」）。
  システム外のQRを読んだときは「このシステムのQRではないようです」と区別して表示。ベトナム語対応。

### Changed
- **HTML/JS/CSS の配信を no-cache に**：スマホに古い app.js が残って新機能が効かない事故を防止
  （毎回サーバーへ確認するが、変更が無ければ 304 で転送なし＝体感は変わらない）。
- **「QRを読み取る」の説明を「シャッターを押すと読み取ります」に変更**：iPhoneでは撮影方式のため
  「かざすだけ」では反応しない（現場からの報告の主原因）。あわせて「ふつうのカメラアプリを
  かざして出てきたリンクを押してもOK（いちばん確実）」のヒントを追加。ラベルQRの有効性は
  新旧エンコーダとも独立デコーダ2種で検証済み。
- **自動反映の盲点を修正**：サーバーPC上で直接コミットした場合（リモートより遅れ0）でも
  HEAD の変化を検出して自動再起動するように。

---

## [1.13.0] - 2026-09-23
QR読み取りの大幅強化と、QRラベルの印刷済み管理。マニュアルを3部構成に刷新。

### Added
- **QRラベルの「印刷済み」目印**：在庫管理の表に QR 列（✔済／未）を追加。ラベル印刷ダイアログを
  閉じると「印刷済みにしますか？」と確認し、日時つきで記録（`records.qr_printed_at`、
  `POST /api/labeled`）。ダイアログの「未印刷のみ」ボタンで未印刷ラベルだけをまとめて印刷可能。
  少しずつ貼っていく運用向け。
- **使い方マニュアルを3部構成で全面作り直し**（docs/マニュアル）：①在庫があるか確認する
  ②在庫を使う（残りの長さ登録） ③新しい在庫を登録する。実画面スクリーンショット＋部分拡大で構成し、
  文字・図の枠はみ出しを全ページ検査済み。

### Changed
- **スマホの「QRを読み取る」を3段デコーダに強化**：BarcodeDetector（Android標準）→
  zxing-wasm（同梱・オフライン動作）→ jsQR の順に自動フォールバック。斜め・ブレ・低画質・
  遠距離の実写を想定したテスト8種すべて読取OK。ラベルのQRは誤り訂正レベルを M→Q に引き上げ
  （汚れ・かすれに強く）。

### Fixed
- **ベトナム語の声調付き文字（ầ ề ồ ẫ ế 等）が Windows で崩れる問題**：Yu Gothic 系フォントが
  合成済みグリフを持たないため。VI表示時はフォントを Segoe UI 優先に切替え、言語ボタンの
  「Tiếng Việt」表記も常に Segoe UI で描画するよう修正（styles.css のみ、日本語表示は従来どおり）。

---

## [1.12.0] - 2026-09-23
### Added
- **「残材を登録」（現場向け・スマホ対応）**：持ち出し画面のボタンから、納入されたばかりで
  システム未登録の材料の残りをその場で新規登録できる（材質→鋼種→板厚→規格→残りの長さ→
  場所→仕上げ→名前）。名前つきで履歴・作業ログに記録。ベトナム語対応。マニュアルにも追記。

---

## [1.11.1] - 2026-09-23
### Removed
- **「入出庫履歴」タブを削除し「作業ログ」に一本化**（内容が完全に重複していたため）。
  作業ログ＝日付別の全記録（持ち出し・登録・編集・削除・取込・鍵の倉庫入り）＋記録なし⚠監視＋CSV出力。

---

## [1.11.0] - 2026-09-23
運用簡素化：鍵の返却操作を廃止／アルミをA6063に統一／サニタリーは鋼種のみに。

### Changed
- **鍵の返却操作を廃止**：QRで記録するのは「倉庫へ行くとき」だけ（ボタンは「記録して倉庫へ行く」の1つ）。
  鍵はフックに戻すだけ。作業ログの判定は「倉庫入り後、その日のうちに本人の在庫記録が無ければ⚠」
  に変更（倉庫入りから1時間以内は「作業中」扱い）。
- **アルミ系材質を A6063 に統一**：在庫31件（アルミ・A5052）を A6063 に変換、マスタからも削除。
  A6063 の鋼種に化粧管・BA管・フラットバー・丸棒・角棒を追加し、単価は直近1年のアルミ系
  加重平均 1,328円/kg に統合。CIPS単価取込ツールもアルミ系→A6063 に判定変更。
- **表面仕上げから「サニタリー」を削除**（鋼種サニタリーパイプで扱う）。該当在庫16件の仕上げを
  空欄化し、SUS304/SUS316L サニタリーパイプに仕上げ不問の単価行を追加（材料費の表示を維持）。
- 使い方マニュアル（docs/マニュアル）を新仕様に更新。

---

## [1.10.0] - 2026-09-22
CIPS発注実績から平均キロ単価を算出し、単価マスタへ反映。

### Added
- `tools/cips単価取込.js`：CIPS発注明細（単位kg・単価あり・直近1年＝1,885件）から品名を解析し、
  数量加重平均のキロ単価を算出して単価マスタに反映（73件更新）。「確認」引数でドライラン可。
  優先順位：①材質×鋼種の実績(3件以上) → ②同材質の形鋼類平均 → ③同材質全体平均（板材含む）。
  実績のない材質（A6063・SUS310S・チタン）は据え置き。
- `unitPrice()` に「仕上げ不問(null)行」へのフォールバックを追加（完全一致行が無いときのみ。
  既存の完全一致・合算の挙動は不変。docs/CALCULATION.md に追記）。
  これにより**化粧管・BA管・アルミ系にも材料費が表示される**ようになった。

### Changed
- SS400/A6063 は合算仕様のため、単価更新時に複数行を1行へ統合
  （例：SS400フラットバー ミガキ250＋黒皮250=500 → 実勢159円/kg の1行）。

---

## [1.9.0] - 2026-09-21
スマホにQR読み取りボタンを追加。名前の選択肢を名簿だけに整理。

### Added
- **「QRを読み取る」ボタン（スマホの持ち出し画面のみ）**：押すとカメラが起動し、材料ラベルを
  撮影するとその場で解析して該当材料の持ち出し画面（鍵QRなら鍵画面）を直接開く。
  HTTP環境でも動く撮影方式。解析には jsQR（MIT）を `js/jsqr.js` に同梱（CDN不使用・オフライン維持）。

### Changed
- 名前の選択肢は**名簿マスタのみ**から生成（名簿が空のときだけ履歴の名前で代用）。
- 履歴に残っていたテスト名（ああ・テスト検証 等）を消去。

---

## [1.8.1] - 2026-09-21
### Changed
- **スマホ表示を「持ち出し画面だけ」に簡素化**（現場の声を反映）。タブ・黒帯・見出し・
  管理者/使い方ボタンを非表示にし、開くと即持ち出し画面。横はみ出しも解消（横スクロール禁止）。
  前日追加したホーム画面は廃止。QR（?co / ?key）からの直接オープンは従来どおり。

### Fixed
- QRから直接開いたとき、名前の選択肢が空になることがある問題を修正
  （履歴の読み込み完了を待ってからモーダルを開くように変更）。

---

## [1.8.0] - 2026-09-21
スマホ用ホーム画面・タブの権限整理・CIPS名簿取込ツール。

### Added
- **スマホ用ホーム画面**：幅760px以下では大きなボタン3つ（材料を持ち出す／倉庫の鍵／在庫をさがす）
  のシンプルなホームを最初に表示（QRから開いた場合は従来どおり直接該当画面）。ベトナム語対応。
- **CIPS名簿取込**（`tools/cips名簿取込.js`）：CIPSの社員マスタ（master_company_staff・読取専用）
  から名前を取得し、マスタの「名簿」に保存。サーバー機で `node tools\cips名簿取込.js` を実行。

### Changed
- **「在庫管理」「入出庫履歴」タブを管理者のみに変更**。通常時に見えるのは
  ホーム（スマホ）・在庫検索・持ち出しだけ。使い方ガイドの記述も更新。

---

## [1.7.0] - 2026-09-21
名前の選択式入力（名簿マスタ）＋ 日本語／ベトナム語切替。

### Added
- **名前ピッカー**：持ち出し・鍵の名前入力を選択式に変更。選択肢＝マスタ設定の新パネル
  **「名簿」**＋実際に持ち出し/鍵操作をした名前。前回の名前は端末ごとに記憶され**2回目から自動選択**。
  「（名前を直接入力）」を選べば手入力も可能。
- **言語切替（日本語⇄Tiếng Việt）**：ヘッダーの言語ボタンで切替（端末ごとに記憶）。
  翻訳対象は現場向け画面（タブ・在庫検索・持ち出し・鍵・入出庫履歴・主要トースト）。
  管理者向け画面（マスタ設定・作業ログ・計算）と使い方ガイドは日本語のみ。

---

## [1.6.0] - 2026-09-21
鍵の持出管理＋残長方式の登録＋管理者用の作業ログ（日別監視）。

### Added
- **鋼材倉庫の鍵の記録**：鍵置き場に貼るQR（在庫管理→「鍵QR」で印刷）を読むと持出/返却画面が開き、
  「誰がいつ倉庫へ行ったか・返したか」を記録（`GET/POST /api/key`、history に keyout/keyin）。
- **作業ログ（管理者のみの新タブ）**：全記録を日付別に一覧表示。鍵の持出→返却の間に本人の在庫記録が
  1件も無い場合 **「記録なし⚠」** を自動表示（未返却も⚠）。CSV出力つき。
- 鍵の記録は公開の「入出庫履歴」には表示しない（作業ログのみ）。

### Changed
- 持ち出しの一部使用を**「残りの長さを入力」方式に変更**（実測値をそのまま入力→使用量は自動逆算）。
- 材質の表記統一：半角ﾁﾀﾝ → 全角チタン（在庫1件を修正、比重マスタも全角のみに）。

---

## [1.5.0] - 2026-09-21
管理者モードでマスタ（キロ単価・比重・断面積式の割り当て）を画面から編集可能に。

### Added
- **マスタ設定画面**（旧マスタ参照・管理者のみ）：キロ単価の行追加/削除/単価変更、材質別比重の編集、
  鋼種ごとの「適用する断面積式」の選択（7種類から。式の中身は固定＝Excel再現を維持）。
  「マスタを保存」でサーバーDBに保存され**全PCへ自動反映**（ポーリング検知）。「初期値に戻す」で既定値へ。
- API：`GET /api/masters`、`PUT /api/masters`（管理者パスコード必須・403検証済み）。
  単体ファイル版は localStorage（`steel_mgr_masters_v1`）に保存。

### Changed
- `sectionArea()` を式マップ駆動に内部リファクタ（**数式・計算結果は完全に同一**。
  化粧管12.646kg／丸棒0.645kg／SS400フラットバー合算500円 の一致を検証済み）。
  `FORMULA_DESC` は `AREA_FORMULAS`＋`KOSHU_FORMULA` に統合。`DENSITY`/`PRICE` は上書き可能に。

---

## [1.4.1] - 2026-09-21
### Changed
- **選択肢を在庫の実データから動的生成**：新規登録／編集モーダル・在庫検索・持ち出しの
  「材質・鋼種・板厚・材料規格候補・表面仕上げ」を、固定マスタ＋実在庫に出てくる値の合算に変更。
  在庫に無い値はマスタどおり、在庫にだけある値（例：板厚0.5／材質ﾁﾀﾝ／仕上げ内面＃400）も選べる。
  鋼種→規格候補の連動も実在庫の規格を反映（例：化粧管はΦ114.3等36種）。

---

## [1.4.0] - 2026-09-21
紙台帳（画像読取Excel）から実在庫545件を取込。マスタを実データに合わせて拡張。

### Added
- 鋼種を追加：**化粧管・BA管・丸パイプ(種別不明)**（丸管と同じ断面積式）、**丸棒・角棒**（無垢材の式）。
  断面形状アイコン・計算式マスタも追加。詳細は docs/CALCULATION.md の追記を参照。
- 材質を追加：A5052・アルミ（比重2.7）・ﾁﾀﾝ（比重4.51）。表面仕上げ候補に サニタリー・BA。
- **保管場所の候補を在庫の実データから動的生成**（A-1〜H-7 等55か所に対応。ハードコード廃止）。

### Changed
- 在庫データを一括入替：旧データ10件を削除（バックアップ：`server/data/backup_before_import_20260921.json`）し、
  `材料在庫_画像読取結果.xlsx` から545件を取込（556行中、空行・抹消行10件はスキップ、「×2本」は2件に展開）。
  規格の表記は `×`→`*`・`φ`→`Φ` に統一。板厚不明の12件は板厚0で取込（要手修正）。
  読取時の要確認28件（Q001〜Q034 の一部）は読取値のまま取込。

---

## [1.3.2] - 2026-07-18
「本番に反映」ボタンを撤去し、GitHub 監視による全自動反映に変更。

### Added
- **GitHub 自動反映**：サーバーが約3分ごとに origin を確認し、新しいコミットがあれば
  `git pull --ff-only` → 自動再起動（環境変数 `AUTO_UPDATE_SEC` で間隔変更、`0` で無効）。
- クライアントの自動再読み込み：`/api/state` にサーバー起動時刻 `boot` を追加。
  変化を検知したら再読み込み（モーダル入力中は閉じるまで保留）。

### Removed
- 「本番に反映」ボタンと `POST /api/deploy`（自動化により不要）。

---

## [1.3.1] - 2026-07-18
### Added
- **「本番に反映」ボタン**（管理者モード時のみヘッダーに表示・サーバー版のみ）：
  どのPCからでも GitHub の最新版を本番サーバーに反映できる。
  `POST /api/deploy`（パスコード必須）→ サーバー機で `git pull --ff-only` → 更新があれば
  プロセス終了 → 常駐ループ（server-daemon.bat）が新コードで自動再起動 → 画面も自動再読み込み。

---

## [1.3.0] - 2026-07-18
QRラベル：材料に貼ったQRをスマホで読むと、その在庫の持ち出し画面が直接開く。

### Added
- **QRラベル印刷**（在庫管理 → 「QRラベル」で全件、各行のQRボタンで1枚ずつ）。
  ラベルには QR＋材質/鋼種/規格/長さ/場所/No. を記載。印刷ボタンでラベルだけを印刷（3列レイアウト）。
- QRの中身は `http://<サーバーIP>:3001/?co=<在庫ID>`。読み取ると持ち出し画面＋該当在庫のモーダルが直接開く。
  使い切り済み（レコード削除済み）の場合は「見つかりません」と案内。
- **QRコード生成器を素のJSで自前実装**（外部ライブラリ不使用の方針を維持）。
  バイトモード／誤り訂正レベルM／バージョン1〜10自動選択／マスク自動評価。
  検証：デコーダ(jsQR・開発時のみ使用)との往復テスト 132ケース（v1〜v10）全合格。
- `/api/health` にサーバーのLAN側ベースURL（`base`）を追加（QRのリンク先に使用）。

### 注意
- QRラベルはサーバー版のみ（単体ファイル版ではURLが存在しないため非表示）。
- サーバーのIPアドレスが変わるとQRのリンクが切れるため、固定IP（DHCP予約）を推奨。

---

## [1.2.0] - 2026-07-18
現場の「使用報告が上がってこない」対策：持ち出し登録＋入出庫履歴。

### Added
- **「持ち出し」タブ（現場向け・全員使用可）**：材料を選んで「全部 持ち出す／一部だけ使う（使う長さを入力）」
  →名前を入れて記録。在庫の残り長さは自動更新（全部使用はレコード削除）。名前はこの端末に記憶され次回から自動入力。
- **「入出庫履歴」タブ**：誰が・いつ・何を・どれだけ使ったかの一覧＋CSV出力。
  持ち出しに加え、在庫の登録・編集・削除・CSV取込もサーバー側で自動記録（`history` テーブル、既存DBに自動追加）。
- API：`POST /api/checkout`（名前必須・長さ超過は拒否）、`GET /api/history`。
- スマホ表示対応（幅760px以下でカード1列・ボタン大型化）。倉庫でスマホ/タブレットから記録する運用を想定。
- 単体ファイル版（localStorage モード）でも持ち出し・履歴が動作（履歴は `steel_mgr_history_v1` に保存）。

### Changed
- 使い方ガイドに「持ち出し」「入出庫履歴」の説明を追加し、保存先の説明をサーバー共有の実態に合わせて更新。

---

## [1.1.2] - 2026-07-18
### Changed
- 管理者パスコード（`ADMIN_PIN`）を既定値 `1234` から変更（値はソース `js/app.js` 参照）。単体版 dist にも同一修正を適用。

---

## [1.1.1] - 2026-07-15
サーバーの常駐化（落ちても自動復帰）。

### Added
- `server-daemon.bat`：サーバーが異常終了しても10秒後に自動で再起動する常駐ループ。
  ポート3001が使用中なら二重起動せずに終了する。
- `サーバー停止.bat`：サーバーと常駐ループをまとめて停止（次回ログオンまで停止したまま）。

### Changed
- `スタートアップ\kyoshin-steel.vbs`：node 直接起動 → `server-daemon.bat` 経由の起動に変更。
  ログオン時に隠しウィンドウで常駐開始。クラッシュ→自動復帰の動作確認済み。

---

## [1.1.0] - 2026-06-09
社内共有サーバー版を追加（localStorage 単体 → サーバー集約で全PC共有）。

### Added
- **共有サーバー**：`server/index.js`（Express）＋ `server/db.js`（SQLite `server/data/app.db`）。在庫を1か所に集約し、`http://192.168.1.107:3001/` で全PCからアクセス。
- 在庫 API：`GET /api/state?since=v`（差分ポーリング）、`POST/PUT/DELETE /api/records`、`POST /api/records/bulk`（CSV取込）、`GET /api/health`。
- クライアントの二重モード：サーバー配信時は REST API ＋約5秒ポーリングで全PC同期、`file://`（単体版 dist）では従来の localStorage に自動フォールバック。
- ログオン時自動起動 `スタートアップ\kyoshin-steel.vbs`（隠しコンソール）。

### Changed
- `js/app.js` の状態層（`loadRecords`/`saveModal`/`delRecord`/`importCSV`/`updateFoot`/`init`）をサーバー連携に対応。**計算ロジック・画面・管理者モードは変更なし**。
- 既定の保存先：サーバー版は `server/data/app.db`、単体版は従来どおり localStorage。

---

## [1.0.0] - 2026-06-08
鋼材管理システムの初版。Excel（`新_鋼材管理Excel.xlsm`）からの移行。

### 機能
- **在庫検索**：材質→鋼種→規格などの連動絞り込み、合計重量・合計金額・件数の集計、CSV 出力。
- **在庫管理**：在庫の追加／編集／削除（重量・キロ単価・材料費は自動計算）、CSV 取込／出力。
- **重量・単価計算**：単品の試算。断面積・重量・比重・キロ単価・材料費＋適用式を表示。
- **マスタ参照**：比重・キロ単価ルール・断面積式・規格候補の一覧。
- **管理者モード**：パスコード（既定 `1234`）で「計算」「マスタ参照」を表示。`?` キーで使い方ガイド。

### 計算
- Excel の計算ロジックを再現（断面積・重量・キロ単価＝SUMIFS 合算・材料費）。
- サンプル在庫 10 件の重量が Excel と一致することを確認。

### UI / デザイン
- Kyo-shin ブランドに合わせた赤＋黒＋白の配色、上部ヘッダー型レイアウト、ピル型タブ。
- ロゴを背景透過 PNG で埋め込み。
- 親しみやすさ向上：角丸・カラフルな KPI カード・材質別カラータグ・鋼種の断面形状アイコン・
  各所のアイコン・丸ゴシック系フォント優先。

### 保存
- 在庫データは localStorage（キー `steel_mgr_records_v1`）に保存。バックアップは CSV。

### 開発・引き継ぎ
- 単一 HTML を `index.html` / `styles.css` / `js/app.js` / `assets/logo.png` に分割。
- `build.py` で配布用の 1 ファイル版（`dist/鋼材管理システム.html`）を生成。
- ドキュメント（README, docs/CALCULATION.md）を整備。

---

## 記入テンプレート（次回以降）

```
## [x.y.z] - YYYY-MM-DD
### Added     追加機能
### Changed   変更
### Fixed     修正
### Removed    削除
```
