# 変更履歴（CHANGELOG）

形式は緩やかに [Keep a Changelog] に倣います。日付は JST。

## [1.8.0] - 2026-09-21
スマホ用ホーム画面・タブの権限整理・CIPS名簿取込ツール。

### Added
- **スマホ用ホーム画面**：幅760px以下では大きなボタン3つ（材料を持ち出す／倉庫の鍵／在庫をさがす）
  のシンプルなホームを最初に表示（QRから開いた場合は従来どおり直接該当画面）。ベトナム語対応。
- **CIPS名簿取込**（`tools/cips名簿取込.js`）：CIPSの社員マスタ（master_company_staff・読取専用）
  から名前を取得し、マスタの「名簿」に保存。サーバー機で `node tools\cips名簿取込.js` を実行。

### Changed
- **「在庫管理」「入出庫履歴」タブを管理者のみに変更**。通常時に見えるのは
  ホーム（スマホ）・在庫検索・持ち出しだけ。使い方ガイドの記述も更新。

---

## [1.7.0] - 2026-09-21
名前の選択式入力（名簿マスタ）＋ 日本語／ベトナム語切替。

### Added
- **名前ピッカー**：持ち出し・鍵の名前入力を選択式に変更。選択肢＝マスタ設定の新パネル
  **「名簿」**＋実際に持ち出し/鍵操作をした名前。前回の名前は端末ごとに記憶され**2回目から自動選択**。
  「（名前を直接入力）」を選べば手入力も可能。
- **言語切替（日本語⇄Tiếng Việt）**：ヘッダーの言語ボタンで切替（端末ごとに記憶）。
  翻訳対象は現場向け画面（タブ・在庫検索・持ち出し・鍵・入出庫履歴・主要トースト）。
  管理者向け画面（マスタ設定・作業ログ・計算）と使い方ガイドは日本語のみ。

---

## [1.6.0] - 2026-09-21
鍵の持出管理＋残長方式の登録＋管理者用の作業ログ（日別監視）。

### Added
- **鋼材倉庫の鍵の記録**：鍵置き場に貼るQR（在庫管理→「鍵QR」で印刷）を読むと持出/返却画面が開き、
  「誰がいつ倉庫へ行ったか・返したか」を記録（`GET/POST /api/key`、history に keyout/keyin）。
- **作業ログ（管理者のみの新タブ）**：全記録を日付別に一覧表示。鍵の持出→返却の間に本人の在庫記録が
  1件も無い場合 **「記録なし⚠」** を自動表示（未返却も⚠）。CSV出力つき。
- 鍵の記録は公開の「入出庫履歴」には表示しない（作業ログのみ）。

### Changed
- 持ち出しの一部使用を**「残りの長さを入力」方式に変更**（実測値をそのまま入力→使用量は自動逆算）。
- 材質の表記統一：半角ﾁﾀﾝ → 全角チタン（在庫1件を修正、比重マスタも全角のみに）。

---

## [1.5.0] - 2026-09-21
管理者モードでマスタ（キロ単価・比重・断面積式の割り当て）を画面から編集可能に。

### Added
- **マスタ設定画面**（旧マスタ参照・管理者のみ）：キロ単価の行追加/削除/単価変更、材質別比重の編集、
  鋼種ごとの「適用する断面積式」の選択（7種類から。式の中身は固定＝Excel再現を維持）。
  「マスタを保存」でサーバーDBに保存され**全PCへ自動反映**（ポーリング検知）。「初期値に戻す」で既定値へ。
- API：`GET /api/masters`、`PUT /api/masters`（管理者パスコード必須・403検証済み）。
  単体ファイル版は localStorage（`steel_mgr_masters_v1`）に保存。

### Changed
- `sectionArea()` を式マップ駆動に内部リファクタ（**数式・計算結果は完全に同一**。
  化粧管12.646kg／丸棒0.645kg／SS400フラットバー合算500円 の一致を検証済み）。
  `FORMULA_DESC` は `AREA_FORMULAS`＋`KOSHU_FORMULA` に統合。`DENSITY`/`PRICE` は上書き可能に。

---

## [1.4.1] - 2026-09-21
### Changed
- **選択肢を在庫の実データから動的生成**：新規登録／編集モーダル・在庫検索・持ち出しの
  「材質・鋼種・板厚・材料規格候補・表面仕上げ」を、固定マスタ＋実在庫に出てくる値の合算に変更。
  在庫に無い値はマスタどおり、在庫にだけある値（例：板厚0.5／材質ﾁﾀﾝ／仕上げ内面＃400）も選べる。
  鋼種→規格候補の連動も実在庫の規格を反映（例：化粧管はΦ114.3等36種）。

---

## [1.4.0] - 2026-09-21
紙台帳（画像読取Excel）から実在庫545件を取込。マスタを実データに合わせて拡張。

### Added
- 鋼種を追加：**化粧管・BA管・丸パイプ(種別不明)**（丸管と同じ断面積式）、**丸棒・角棒**（無垢材の式）。
  断面形状アイコン・計算式マスタも追加。詳細は docs/CALCULATION.md の追記を参照。
- 材質を追加：A5052・アルミ（比重2.7）・ﾁﾀﾝ（比重4.51）。表面仕上げ候補に サニタリー・BA。
- **保管場所の候補を在庫の実データから動的生成**（A-1〜H-7 等55か所に対応。ハードコード廃止）。

### Changed
- 在庫データを一括入替：旧データ10件を削除（バックアップ：`server/data/backup_before_import_20260921.json`）し、
  `材料在庫_画像読取結果.xlsx` から545件を取込（556行中、空行・抹消行10件はスキップ、「×2本」は2件に展開）。
  規格の表記は `×`→`*`・`φ`→`Φ` に統一。板厚不明の12件は板厚0で取込（要手修正）。
  読取時の要確認28件（Q001〜Q034 の一部）は読取値のまま取込。

---

## [1.3.2] - 2026-07-18
「本番に反映」ボタンを撤去し、GitHub 監視による全自動反映に変更。

### Added
- **GitHub 自動反映**：サーバーが約3分ごとに origin を確認し、新しいコミットがあれば
  `git pull --ff-only` → 自動再起動（環境変数 `AUTO_UPDATE_SEC` で間隔変更、`0` で無効）。
- クライアントの自動再読み込み：`/api/state` にサーバー起動時刻 `boot` を追加。
  変化を検知したら再読み込み（モーダル入力中は閉じるまで保留）。

### Removed
- 「本番に反映」ボタンと `POST /api/deploy`（自動化により不要）。

---

## [1.3.1] - 2026-07-18
### Added
- **「本番に反映」ボタン**（管理者モード時のみヘッダーに表示・サーバー版のみ）：
  どのPCからでも GitHub の最新版を本番サーバーに反映できる。
  `POST /api/deploy`（パスコード必須）→ サーバー機で `git pull --ff-only` → 更新があれば
  プロセス終了 → 常駐ループ（server-daemon.bat）が新コードで自動再起動 → 画面も自動再読み込み。

---

## [1.3.0] - 2026-07-18
QRラベル：材料に貼ったQRをスマホで読むと、その在庫の持ち出し画面が直接開く。

### Added
- **QRラベル印刷**（在庫管理 → 「QRラベル」で全件、各行のQRボタンで1枚ずつ）。
  ラベルには QR＋材質/鋼種/規格/長さ/場所/No. を記載。印刷ボタンでラベルだけを印刷（3列レイアウト）。
- QRの中身は `http://<サーバーIP>:3001/?co=<在庫ID>`。読み取ると持ち出し画面＋該当在庫のモーダルが直接開く。
  使い切り済み（レコード削除済み）の場合は「見つかりません」と案内。
- **QRコード生成器を素のJSで自前実装**（外部ライブラリ不使用の方針を維持）。
  バイトモード／誤り訂正レベルM／バージョン1〜10自動選択／マスク自動評価。
  検証：デコーダ(jsQR・開発時のみ使用)との往復テスト 132ケース（v1〜v10）全合格。
- `/api/health` にサーバーのLAN側ベースURL（`base`）を追加（QRのリンク先に使用）。

### 注意
- QRラベルはサーバー版のみ（単体ファイル版ではURLが存在しないため非表示）。
- サーバーのIPアドレスが変わるとQRのリンクが切れるため、固定IP（DHCP予約）を推奨。

---

## [1.2.0] - 2026-07-18
現場の「使用報告が上がってこない」対策：持ち出し登録＋入出庫履歴。

### Added
- **「持ち出し」タブ（現場向け・全員使用可）**：材料を選んで「全部 持ち出す／一部だけ使う（使う長さを入力）」
  →名前を入れて記録。在庫の残り長さは自動更新（全部使用はレコード削除）。名前はこの端末に記憶され次回から自動入力。
- **「入出庫履歴」タブ**：誰が・いつ・何を・どれだけ使ったかの一覧＋CSV出力。
  持ち出しに加え、在庫の登録・編集・削除・CSV取込もサーバー側で自動記録（`history` テーブル、既存DBに自動追加）。
- API：`POST /api/checkout`（名前必須・長さ超過は拒否）、`GET /api/history`。
- スマホ表示対応（幅760px以下でカード1列・ボタン大型化）。倉庫でスマホ/タブレットから記録する運用を想定。
- 単体ファイル版（localStorage モード）でも持ち出し・履歴が動作（履歴は `steel_mgr_history_v1` に保存）。

### Changed
- 使い方ガイドに「持ち出し」「入出庫履歴」の説明を追加し、保存先の説明をサーバー共有の実態に合わせて更新。

---

## [1.1.2] - 2026-07-18
### Changed
- 管理者パスコード（`ADMIN_PIN`）を既定値 `1234` から変更（値はソース `js/app.js` 参照）。単体版 dist にも同一修正を適用。

---

## [1.1.1] - 2026-07-15
サーバーの常駐化（落ちても自動復帰）。

### Added
- `server-daemon.bat`：サーバーが異常終了しても10秒後に自動で再起動する常駐ループ。
  ポート3001が使用中なら二重起動せずに終了する。
- `サーバー停止.bat`：サーバーと常駐ループをまとめて停止（次回ログオンまで停止したまま）。

### Changed
- `スタートアップ\kyoshin-steel.vbs`：node 直接起動 → `server-daemon.bat` 経由の起動に変更。
  ログオン時に隠しウィンドウで常駐開始。クラッシュ→自動復帰の動作確認済み。

---

## [1.1.0] - 2026-06-09
社内共有サーバー版を追加（localStorage 単体 → サーバー集約で全PC共有）。

### Added
- **共有サーバー**：`server/index.js`（Express）＋ `server/db.js`（SQLite `server/data/app.db`）。在庫を1か所に集約し、`http://192.168.1.107:3001/` で全PCからアクセス。
- 在庫 API：`GET /api/state?since=v`（差分ポーリング）、`POST/PUT/DELETE /api/records`、`POST /api/records/bulk`（CSV取込）、`GET /api/health`。
- クライアントの二重モード：サーバー配信時は REST API ＋約5秒ポーリングで全PC同期、`file://`（単体版 dist）では従来の localStorage に自動フォールバック。
- ログオン時自動起動 `スタートアップ\kyoshin-steel.vbs`（隠しコンソール）。

### Changed
- `js/app.js` の状態層（`loadRecords`/`saveModal`/`delRecord`/`importCSV`/`updateFoot`/`init`）をサーバー連携に対応。**計算ロジック・画面・管理者モードは変更なし**。
- 既定の保存先：サーバー版は `server/data/app.db`、単体版は従来どおり localStorage。

---

## [1.0.0] - 2026-06-08
鋼材管理システムの初版。Excel（`新_鋼材管理Excel.xlsm`）からの移行。

### 機能
- **在庫検索**：材質→鋼種→規格などの連動絞り込み、合計重量・合計金額・件数の集計、CSV 出力。
- **在庫管理**：在庫の追加／編集／削除（重量・キロ単価・材料費は自動計算）、CSV 取込／出力。
- **重量・単価計算**：単品の試算。断面積・重量・比重・キロ単価・材料費＋適用式を表示。
- **マスタ参照**：比重・キロ単価ルール・断面積式・規格候補の一覧。
- **管理者モード**：パスコード（既定 `1234`）で「計算」「マスタ参照」を表示。`?` キーで使い方ガイド。

### 計算
- Excel の計算ロジックを再現（断面積・重量・キロ単価＝SUMIFS 合算・材料費）。
- サンプル在庫 10 件の重量が Excel と一致することを確認。

### UI / デザイン
- Kyo-shin ブランドに合わせた赤＋黒＋白の配色、上部ヘッダー型レイアウト、ピル型タブ。
- ロゴを背景透過 PNG で埋め込み。
- 親しみやすさ向上：角丸・カラフルな KPI カード・材質別カラータグ・鋼種の断面形状アイコン・
  各所のアイコン・丸ゴシック系フォント優先。

### 保存
- 在庫データは localStorage（キー `steel_mgr_records_v1`）に保存。バックアップは CSV。

### 開発・引き継ぎ
- 単一 HTML を `index.html` / `styles.css` / `js/app.js` / `assets/logo.png` に分割。
- `build.py` で配布用の 1 ファイル版（`dist/鋼材管理システム.html`）を生成。
- ドキュメント（README, docs/CALCULATION.md）を整備。

---

## 記入テンプレート（次回以降）

```
## [x.y.z] - YYYY-MM-DD
### Added     追加機能
### Changed   変更
### Fixed     修正
### Removed    削除
```
